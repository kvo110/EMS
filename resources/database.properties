# Database Configuration Properties
# TODO: Configure database connection settings

# Database Connection Settings
db.url=jdbc:mysql://localhost:3306/employeeData
db.driver=com.mysql.cj.jdbc.Driver

# HR Admin Database Credentials (Full Access)
db.admin.username=hr_admin
db.admin.password=CHANGE_ME_SECURE_PASSWORD

# General Employee Database Credentials (Read-Only Access)  
db.employee.username=general_employee
db.employee.password=CHANGE_ME_EMPLOYEE_PASSWORD

# Connection Pool Settings
db.pool.minSize=5
db.pool.maxSize=20
db.pool.timeout=30000

# Security Settings
db.ssl.enabled=true
db.ssl.trustStore=path/to/truststore
db.ssl.trustStorePassword=CHANGE_ME_TRUSTSTORE_PASSWORD

# Application Settings
app.session.timeout=1800
app.max.login.attempts=3
app.password.min.length=8
